
		item := items[index]
		firstchar := ([]rune(items[index]))[0]
    isarrayparam := false
    if firstchar == '*' {
			_, isarrayparam = getArrayLen(item) 
    }



		//if we're not in an array and the first char is a star, and the start is followed by a number, then it's an arraay.
		if isarrayparam && arraylen == 0 {
			bytecount += len([]byte(item)) + 2 
			arraylen, _ = getArrayLen(item)
			// if isarrayparam {
			// 	return []CommandStringsContainer{}, errors.New("Could not parse array length")
			// }
			continue
		}
